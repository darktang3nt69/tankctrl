---
title: PHASE2_NOTIFICATION_PREFERENCES
type: note
permalink: tankctl/docs/backend/phase2-notification-preferences
---

# Phase 2: Notification Preferences Integration---


































































































































































































































































**Status:** ✅ Complete - Ready for testing and integrationPhase 2 introduces per-schedule notification preferences, allowing users fine-grained control over water change reminders while maintaining full backward compatibility. The implementation is production-ready with comprehensive logging and error handling.## Summary```    pass    # Test code heredef test_reminders_with_preferences(mock_push_service):@patch('src.infrastructure.scheduler.scheduler.PushNotificationService')from unittest.mock import Mock, patch# Mock the PushNotificationService in tests```pythonTo test without Firebase:### Testing locally3. Check logs with `docker logs tankctl-backend` or similar2. Verify logger is configured and not filtered1. Ensure APP_TIMEZONE is set correctly (default: Asia/Kolkata)### Logs not showing4. Review scheduler logs for errors3. Check FCM credentials and service account2. Verify push token is registered for the device1. Check `notify_*` columns are TRUE for the schedule### Reminders not firing## Troubleshooting5. **Multi-channel notifications** — Email, SMS, in-app notifications (in addition to FCM)4. **Smart reminders** — Suppress reminders based on user activity patterns3. **Delivery history** — Store reminders sent to users for audit trails2. **Alert tracking** — Track which reminders were sent and when users acknowledged them1. **Global notification preferences** — User-level settings for timezone, quiet hours, default preferences## Future Improvements (Phase 3+)All notify_* columns default to TRUE, so existing water schedules automatically have all reminders enabled. No data migration is required.## Backward Compatibility```pytest tests/test_water_schedule_reminders.py::TestNotificationPreferencesFiltering -v```bash### Running Tests7. **test_custom_schedule_with_preferences** — Verifies custom schedules respect preferences6. **test_selective_reminder_filtering** — Verifies only enabled reminders fire in mixed scenarios5. **test_all_preferences_enabled** — Verifies all reminders fire when all notify_*=True (backward compatibility)4. **test_all_preferences_disabled** — Verifies no reminders fire when all notify_*=False3. **test_on_time_disabled** — Verifies on_time reminder is skipped when notify_on_time=False2. **test_hour_before_disabled** — Verifies hour_before reminder is skipped when notify_1h=False1. **test_day_before_disabled** — Verifies day_before reminder is skipped when notify_24h=FalseTests include:New test class: `TestNotificationPreferencesFiltering` in `tests/test_water_schedule_reminders.py`### Phase 2 Tests## Testing```}  ...  "notify_on_time": false,  "notify_1h": true,  "notify_24h": true,  "schedule_time": "15:00",  "days_of_week": "1,3,5",  "schedule_type": "weekly",  "device_id": "tank1",  "id": 42,{```jsonResponse includes:### GET /api/devices/{device_id}/water-schedules/{schedule_id}```}  "notify_on_time": true  "notify_1h": true,  "notify_24h": false,{```jsonTo update notification preferences:### PUT /api/devices/{device_id}/water-schedules/{schedule_id}```}  "notify_on_time": false  "notify_1h": true,  "notify_24h": true,  "notes": "Weekly water change",  "schedule_time": "15:00",  "days_of_week": "1,3,5",  "schedule_type": "weekly",{```jsonWhen creating a water schedule, optionally include:### POST /api/devices/{device_id}/water-schedulesTo let users control these preferences, the following API endpoints need to be added or updated:## API Updates Required```water_reminder_error schedule_id=42 reminder_type=on_time device_id=tank1 error="..."```If there's an error sending a reminder:### Error Logging```water_reminder_sent device_id=tank1 schedule_id=42 reminder_type=hour_before sent=1```When a reminder is successfully sent:### Send Logging```water_reminder_skipped_preference device_id=tank1 schedule_id=42 reminder_type=day_before preference=notify_24h reason=notify_24h=False```When a reminder is skipped due to user preferences:### Skip Logging## Logging```)    notification_type="water_change",    data=data,    schedule.device_id, title, body,sent = push_service.broadcast_fcm(}    "device_id": schedule.device_id,    "schedule_id": str(schedule.id),    "reminder_type": reminder_type,data = {```python2. **Passes reminder_type to FCM:** Includes reminder type in the data payload so the app knows which type of reminder was sent.```            )                reason=f"{pref_name}=False",                preference=pref_name,                reminder_type=reminder_type,                schedule_id=schedule.id,                device_id=schedule.device_id,                "water_reminder_skipped_preference",            logger.info(        if not pref_value:        pref_value = getattr(schedule, pref_name, True)    for reminder_type, pref_name in reminder_prefs.items():for schedule in schedules:# Log skipped reminders (due to preferences being False)```python1. **Logs skipped reminders:** For each schedule and reminder type, checks if it's disabled in preferences and logs it.**Updated method: `_check_water_schedule_reminders_job()`**### `src/infrastructure/scheduler/scheduler.py`## Scheduler Changes```    return due                due.append((schedule, reminder_type))            if self._should_fire(schedule, reminder_type, offset, now):                            continue            if not self._is_reminder_enabled(schedule, reminder_type):            # Check if this reminder type is enabled in preferences        for reminder_type, offset in REMINDER_OFFSETS.items():            continue        if not schedule.enabled or schedule.completed:    for schedule in schedules:    due: list[tuple[WaterScheduleModel, str]] = []    now = now_in_app_timezone()    """    Filters by notification preferences (notify_24h, notify_1h, notify_on_time).        """Return (schedule, reminder_type) pairs that should fire right now.def get_due_reminders(self, schedules: list[WaterScheduleModel]) -> list[tuple[WaterScheduleModel, str]]:```pythonNow filters reminders by notification preferences before returning them.**Updated method: `get_due_reminders()`**```    return False        return schedule.notify_on_time    elif reminder_type == "on_time":        return schedule.notify_1h    elif reminder_type == "hour_before":        return schedule.notify_24h    if reminder_type == "day_before":    """Check if a reminder type is enabled in the schedule's notification preferences."""def _is_reminder_enabled(self, schedule: WaterScheduleModel, reminder_type: str) -> bool:```pythonChecks if a reminder type is enabled in the schedule's notification preferences.**New method: `_is_reminder_enabled(schedule, reminder_type)`**### `src/services/water_schedule_reminder_service.py`## Service Changes```notify_on_time = Column(Boolean, default=True)  # At-time remindernotify_1h = Column(Boolean, default=True)   # 1-hour before remindernotify_24h = Column(Boolean, default=True)  # 24-hour before reminder```pythonWaterScheduleModel now includes:### Model: `src/infrastructure/db/models.py`All default to TRUE for backward compatibility (all reminders enabled by default).- `notify_on_time` (BOOLEAN, default TRUE) — Enable/disable at-time reminder- `notify_1h` (BOOLEAN, default TRUE) — Enable/disable 1-hour before reminder  - `notify_24h` (BOOLEAN, default TRUE) — Enable/disable 24-hour before reminderAdds three boolean columns to the `water_schedules` table:### Migration: `011_add_water_schedule_notification_preferences.sql`## Database SchemaPhase 2 integrates user notification preferences into the water schedule reminder service and scheduler. Users can now control which reminders they want to receive for each water schedule (24-hour before, 1-hour before, or at scheduled time).## Overviewtitle: PHASE2_NOTIFICATION_PREFERENCES
type: note
permalink: tankctl/docs/backend/phase2-notification-preferences
---