# TankCtl Architecture

(moved from docs/architecture.md)

TankCtl is a self-hosted IoT controller for managing water tank devices. It communicates with devices over MQTT, stores operational state in PostgreSQL, stores telemetry in TimescaleDB, and visualizes metrics using Grafana.

(See original file for full content.)