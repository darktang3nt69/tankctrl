/** Icon barrel over lucide-react — keeps call sites (AppShell, SearchFilterBar,
 * EmptyState, Tabs) stable while giving every icon in the app a shared, much
 * larger set to draw from instead of one-off hand-rolled SVGs. */
export {
  LayoutGrid as IconOverview,
  Bell as IconAlerts,
  Settings as IconSettings,
  Search as IconSearch,
  Inbox as IconInbox,
  Lightbulb as IconLight,
  Cable as IconRelay,
  Droplet as IconWater,
  Terminal as IconCommands,
  RefreshCw as IconReconnecting,
  WifiOff as IconOffline,
} from 'lucide-react'
